## ADDED Requirements

<!-- No new requirements. This is a bug fix that restores existing mouse interaction behavior
     by removing the problematic QQuickWindow::setDefaultAlphaBuffer(true) call. -->
